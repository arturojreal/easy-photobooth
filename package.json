{
  "name": "xr-motion-photobooth",
  "version": "1.0.0",
  "description": "XR Motion Photobooth with cloud storage",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "cloudinary": "^2.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
